# babelmap style.toml — auto-seeded. The section headers below are active,
# but every value line is commented out, so the file stays a no-op until you
# uncomment lines: each commented value already equals the built-in default,
# reconstructed straight from the style registry. Edit, uncomment, save, then
# run reload-style in-app to apply changes live (or turn on config.toml's
# watch_style to auto-reload on save).
#
# Color values accept: a named color (cyan, dark-gray, light-blue, …),
# palette:N (0-15 from the active scheme), #rrggbb hex, a 256-index ("17"),
# or background / foreground (the scheme's bg/fg).

version = 1   # style schema version — do not remove; lets babelmap flag an out-of-date file

# scheme = "tomorrow-night"   # optional base: built-in name or a Ghostty theme path; omit for terminal colours

# ── Roles: the 7 roots everything else derives from.
[roles]
# text    = { fg = "foreground" }                     # body ink on the page (scheme foreground)
# chrome  = { fg = "foreground", bg = "background" }  # ink on a UI surface: bars, panels, upper window
# line    = { fg = "palette:6" }                      # lines, frames, rules, dividers (scheme cyan slot)
# accent  = { fg = "palette:6" }                      # highlights: links, selection, current room, badges
# muted   = { fg = "palette:8" }                      # dim / secondary text (scheme bright-black slot)
# alert   = { fg = "palette:3" }                      # warnings and errors (scheme yellow slot)
# heading = { fg = "foreground", bold = true }        # titles and headers (bold)

# ═══ TEXT — foreground colour + emphasis (these selectors have no surface of their own) ═══

# ── Elements: app text + host surfaces (status/help bars, upper window,
# story browser). Every line already equals its default.
[elements]
# transcript = { parent = "text" }
# status_bar = { parent = "chrome", reversed = true }
# help_bar = { parent = "chrome", reversed = true }
# upper_window = { parent = "chrome" }
# story_info = { parent = "chrome" }
# status_header = { parent = "heading", bg = "black" }
# story_title = { parent = "heading" }
# input_line = { parent = "line" }
# suggestion_line = { parent = "line" }
# scrollbar = { parent = "line" }
# transcript_location = { parent = "accent" }
# story_badge = { parent = "text" }
# badge_zcode = "Z"   # story-list badge: a Z-machine story
# badge_glulx = "G"   # story-list badge: a Glulx story
# badge_blorb = "B"   # story-list badge: resources in a Blorb
# badge_save = "S"   # story-list badge: the story has a save
# badge_hint = "H"   # story-list badge: hints are installed
# badge_hint_available = "h"   # story-list badge: hints exist but aren't downloaded
# hyperlink = { parent = "accent", underline = true }
# story_info_label = { parent = "muted" }
# suggestion = { parent = "muted" }
# transcript_meta = { parent = "muted", glyph = "▏" }
# transcript_warning = { parent = "alert", glyph = "!" }
# transcript_crash = { parent = "alert", bold = true }
# more_prompt = { parent = "chrome", reversed = true }
# tidy_progress = { parent = "accent" }
# meta_marker = { parent = "muted" }
# inventory_dock = { parent = "accent" }
# story_info_title = { parent = "heading" }
# story_info_value = { parent = "text" }
# story_info_blurb = { parent = "muted", italic = true }
# story_info_link = { parent = "accent", underline = true }
# story_info_cover = { parent = "chrome" }
# graphics = { parent = "chrome" }
# inline_image = { parent = "chrome" }
# story_header = { parent = "muted" }
# story_header_active = { parent = "accent", bold = true }
# story_author = { parent = "text" }
# story_year = { parent = "text" }
# story_rating = { parent = "text" }
# story_no_metadata = { parent = "muted" }
# story_tile = { parent = "text" }
# story_tile_selected = { parent = "accent", bold = true, reversed = true }
# notification = { parent = "accent", reversed = true }
# hotkey_key = { parent = "accent" }
# sound_beep_high = { fg = "#ffb428" }
# sound_beep_low = { fg = "#3c8cdc" }
# transcript_input = { parent = "accent" }
# transcript_system = { parent = "muted" }
# warning_marker = { parent = "alert" }
# input_text = { parent = "text" }
# input_prompt = { parent = "text" }
# upper_window_border = { parent = "line" }
# room_panel = { parent = "accent", reversed = true }
# palette_query = { parent = "text" }
# palette_name = { parent = "text" }
# palette_match = { parent = "accent", bold = true }
# palette_desc = { parent = "muted" }
# palette_selected = { parent = "accent", reversed = true }
# ifdb_result = { parent = "text" }
# ifdb_result_selected = { parent = "accent", bold = true, reversed = true }
# ifdb_result_meta = { parent = "muted" }
# ifdb_download_marker = { parent = "accent", glyph = "⭳" }
# ifdb_download_present = { parent = "muted", glyph = "✓" }
# ifdb_attribution = { parent = "muted", italic = true }
# saves_portable = { parent = "accent", glyph = "↗" }
# saves_host_only = { parent = "muted" }
# "band.column_header" = { parent = "muted" }
# "band.column_header:active" = { parent = "accent", bold = true }
# "band.quick" = { parent = "muted" }
# "band.quick:hover" = { parent = "band.quick", reversed = true }
# "band.group_label" = { parent = "heading" }
# file_browser_cwd = { parent = "alert" }
# file_browser_dir = { parent = "accent" }
# inspector_edge_ok = { fg = "green" }
# inspector_edge_distorted = { fg = "red" }
# transcript_search_highlight = { fg = "black", bg = "yellow" }

# ── The 11 Glk styles for text-buffer windows (base: text).
[glk.buffer]
# normal = { parent = "text" }
# emphasized = { parent = "text", italic = true }
# preformatted = { parent = "text" }
# header = { parent = "heading", bold = true }
# subheader = { parent = "heading", bold = true }
# alert = { parent = "alert", bold = true }
# note = { parent = "muted", italic = true }
# blockquote = { parent = "muted" }
# input = { parent = "accent" }
# user1 = { parent = "text" }
# user2 = { parent = "text" }

# ── The 11 Glk styles for text-grid windows (base: chrome).
[glk.grid]
# normal = { parent = "chrome" }
# emphasized = { parent = "chrome", italic = true }
# preformatted = { parent = "chrome" }
# header = { parent = "heading", bold = true }
# subheader = { parent = "heading", bold = true }
# alert = { parent = "alert", bold = true }
# note = { parent = "muted", italic = true }
# blockquote = { parent = "muted" }
# input = { parent = "accent" }
# user1 = { parent = "chrome" }
# user2 = { parent = "chrome" }

# ── Map colours + glyph-set presets.
[map]
# background = {}
# room = { parent = "text" }
# room_current = { parent = "accent" }
# room_selected = { parent = "accent", reversed = true }
# connector = { parent = "accent" }
# connector_distorted = { fg = "magenta" }
# connector_portal = { parent = "accent" }
# shared_path = { fg = "light-cyan" }
# loc_indicator = { parent = "muted" }
# "matrix.header" = { parent = "heading" }
# "matrix.row:here" = { parent = "accent", reversed = true }
# "matrix.row:selected" = { parent = "accent" }
# "matrix.cell:entrance" = { parent = "text", bold = true }
# "matrix.cell:frontier" = { parent = "muted" }
# "matrix.footnote" = { parent = "muted" }
# "edge:oneway" = { parent = "map.connector" }
# "edge:asym" = { parent = "map.connector" }
# trail = { parent = "muted" }
# box_style = "rounded"   # rounded | thick | double | solid | super-thick | ascii | borderless
# arrow_set = "filled"   # filled | line | nerdfont | nf-bold | nf-box | nf-circle | nf-outline
# portal_icons = "ascii"   # ascii | nerdfont | nerdfont-stairs
# path_style = "light"   # light | heavy | dotted
# portal_path_style = "dotted"   # light | heavy | dotted
# diagonal_corners = true   # false = plain orthogonal corner exits, for fonts without Unicode 13 (🮠🮡🮢🮣)

# ── One glyph at a time. An override beats the preset above it, so you can keep ─
# ── a whole preset and change only the one corner you dislike. Values must be ───
# ── exactly ONE narrow character; anything else is ignored. ─────────────────────
[map.overrides]
# "room.normal.tl" = "┌"     # room box corners/edges: room.<normal|current|selected|portal>.<tl|tr|bl|br|h|v>
# "arrow.north" = "▲"        # connector arrowheads: arrow.<north|south|east|west|ne|nw|se|sw>
# "path.ns" = "│"            # connector lines: path.<ns|ew|ne|nw|se|sw|nse|nsw|ews|ewn|cross>
# "path.diag_ul" = "🮠"       # the four half-diagonals: path.<diag_ul|diag_ur|diag_ll|diag_lr>
# "portal.up" = "↑"          # portal markers: portal.<up|down|in|out|marker|path|unknown>
# "gutter.meta" = "▏"        # transcript gutter marks: gutter.<meta|warning>

# ── Debug inspector disassembly selectors.
[debug]
# pc = { parent = "accent", reversed = true }
# disasm_executed = { parent = "accent", fg = "blue", glyph = "|" }
# disasm_rd = { parent = "text", fg = "yellow", glyph = " " }
# disasm_soft = { parent = "muted", fg = "red", glyph = " " }
# disasm_data = { parent = "muted", italic = true, glyph = " " }

# ═══ SURFACES — bordered boxes: a background + frame + the text drawn on them ═══

# ── Panel: the tiled panes (story/map/verb/debug frames) — background,
# border, title, tabs. Story/Glk windows use [glk.*], not this section.
[panel]
# background = {}
# border = { parent = "line", style = "single" }   # style: none | single | double | thick | rounded
# "border:active" = { parent = "line", bold = true, style = "single" }   # style: none | single | double | thick | rounded
# title = { parent = "heading" }
# tab = { parent = "muted" }
# "tab:active" = { parent = "accent", bold = true }
# tab_divider = { parent = "line", glyph = "│" }
# terminator_left = { parent = "line", glyph = "┤" }
# terminator_right = { parent = "line", glyph = "├" }

# ── Dialog: modal pop-ups — background, its own border, title, buttons, shadow.
[dialog]
# background = { parent = "chrome" }
# border = { parent = "line", bold = true, style = "single" }   # style: none | single | double | thick | rounded
# title = { parent = "accent" }
# button = { parent = "chrome", reversed = true }
# "button:active" = { parent = "accent", reversed = true }
# shadow = { parent = "muted", bg = "dark-gray" }
# list_selected = { fg = "black", bg = "cyan", bold = true }
# list_footer = { parent = "muted" }
# list_header = { parent = "text", underline = true }
# hint_suggestion = { parent = "alert", dim = true }
# input_preview = { parent = "alert" }

# ── Tooltip: hover pop-ups — background and an optional border.
[tooltip]
# background = { parent = "chrome" }
# border = { parent = "line", style = "none" }   # style: none | single | double | thick | rounded

# ── Story-line styling rules: recolour whole transcript lines matching a ────
# ── regex. Rules are tried in order; the first match wins. ──────────────────
# [[transcript.rule]]
# match = "^>.*"                 # your echoed command lines → magenta bold
# fg = "magenta"
# bold = true

# [[transcript.rule]]
# match = "(?i)\\bgrue\\b"       # any line mentioning a "grue" → red (flavour example)
# fg = "red"

# ── Status bar. Omit [statusbar] for the built-in default. ───────────────────
# Placeholders: {location} {score} {moves} {time} {turns} {title} {filter}
# [statusbar]
# border = "none"

# [[statusbar.segment]]
# text  = "{location}"
# align = "left"
# parent = "accent"              # segments may reference a role or set fg/bg directly
# bold  = true

# [[statusbar.segment]]
# text  = "Score: {score}  Moves: {moves}"
# align = "right"

# [[statusbar.segment]]
# text  = "{time}"
# align = "right"
