# Completed Items from TODO

DONE: the opcode-completeness pass (the items marked [PASS]) is COMPLETE — all five core v4+
VAR opcodes are implemented with tests, and the previously-silent VAR fallthrough now records a
one-time diagnostic per unimplemented opcode (surfaced as Warning transcript lines). The only
remaining unimplemented VAR opcode is sound_effect (0x15), tracked separately below (Blorb audio).

1. [x] Add support for "Z-Machine Font 3". E.G. In BeyondZork the story starts with a menu, and the text "Use the ? and ? keys to highlight your selection." You can then use the arrow keys to move up and down. I'm guessing those '?' symbols are supposed to be shown as up and down arrows, so we are missing some extended characters.
1. [x] In BeyondZork, when the startup menu is displayed, the up and down arrows work in the zvm-cli client, but not in the app.
1. [x] The simple cli clients are not handling word wrap properly. I added basic support in zvm-cli, so check my implemenation and add for gvm-cli. Are we also honoring the buffer_mode opcode properly? Please verify for both clients. NOTE (surfaced 2026-07-01 by the BeyondZork cursor fix): zvm-cli's write_counted/wrap_line count SGR escape BYTES as visual columns (e.g. `\x1b[37;40m>\x1b[0m` advances current_col by 13), so styled/coloured text wraps at the wrong column and current_col is unusable for positioning. Track when reworking the wrap path.
1. [x] The simple cli clients are not properly detecting the terminal size, we should also handle resize events. — zvm-cli DONE (5babde2, 2026-07-01): build_machine seeds write_screen_dims() with the detected terminal size and apply_resize keeps the header dims current on resize (was hard-coded 80x24). REMAINING: verify/port the same for gvm-cli. (Distinct from the deferred line-below item re: re-wrapping already-displayed scrollback on resize.)
1. [x] Support page up/down in Story directory list, use animated scrolling (as will all scrolling windows)
1. [x] All window that can scroll should have a scrollbar, all windows with scrollbars should support mouse scroll, fix any and add to memory.
1. [x] Remove score bar completely for v4+ games
1. [x] Support game-driven COLOUR — Phase 1 (Z-MACHINE) DONE (2026-06-30 SDD wave, 15 commits ab3225c..55daaa4 on local main; spec docs/superpowers/specs/2026-06-30-zmachine-colour-design.md, plan docs/superpowers/plans/2026-06-30-zmachine-colour.md). zvm now honors set_colour (2OP:0x1B, per-channel sentinels: 0=keep / 1=default / 2-12=v5 palette + v6 greys) and set_true_colour (EXT:0x0D — NOTE the old code mislabeled 0x05 as set_true_colour; 0x05 is actually draw_picture, now corrected) with 15-bit RGB. Added a zero-dep ZColour type + per-cell/screen fg/bg, a TextAttrs/print_attr output seam, and init_header_caps now GATES the Flags1 colour bit on honor_game_colours (advertise_colour). RENDERING: standard colours 2-9 route through the user's 16-colour scheme palette (ColorScheme.palette, wired from the Ghostty theme) so game "red" = the user's themed red; v6 greys (10-12) + true-colour render as exact RGB (Color::Rgb / 24-bit SGR). zvm-cli renders ANSI SGR (default ON, --no-game-colours opt-out, render gated). App renders colour in the transcript + upper window (packed-u32 fg/bg on StyleRun + GridCell, serde-back-compat), reverse-video swapped exactly once per renderer, upper-window cursor always visible. Config toggle honor_game_colours DEFAULT ON for ALL clients + F2 settings toggle; OFF reproduces the pre-feature look. (The earlier "app default OFF" plan was dropped: since standard colours route THROUGH the theme palette rather than overriding it, ON-by-default is harmonious.) DEFERRED cosmetic cleanups: misleading test comment (T2), combined-SGR test pin (T7), Standard(n) grey-arm lower-bound guard, explicit StyleRun missing-fg/bg deserialize test (T10). [customization+engine]
1. [x] Support game-driven COLOUR — Phase 2 (GLULX/GLK), sub-project 2 — DONE (2026-07-01, commits 8b8eebb..c767091 on local main). gvm now honours Glk STYLE HINTS (glk_stylehint_set with stylehint_TextColor / stylehint_BackColor) per STYLE CLASS: 8b8eebb captures the hints and resolves the active style's colour at output time (was a no-op at exec.rs ~2409); f28973e renders them in gvm-cli as truecolor SGR; d1249d9 renders in the app transcript/windows with full 24-bit fidelity; c767091 gates the Glulx colour at RENDER time (not record time), matching the Phase 1 pattern; bb0439f documents it. Reuses Phase 1's app machinery (StyleRun/GridCell packed-colour threading, honor_game_colours config + F2 toggle). [customization+engine; gvm Glk stylehints + gvm-cli]
1. [x] DONE (zvm-cli DOS-parity, commits a74bd38..f3cabf9, pushed) — HINT command fails with leathergoddesses-invclues-r4-s880405.z5 (at least on zvm-cli). You need to type hint twice. ROOT CAUSE (confirmed): the hint menu renders into the Z-machine UPPER WINDOW, which zvm-cli currently drops entirely (text printed while current_window==1 lands in screen.upper and never reaches the stdout sink). ADDRESSED by the zvm-cli screen-model spec (docs/superpowers/specs/2026-06-27-zvm-cli-screen-model-design.md) — once the upper window is rendered, the hint menu is visible on the first invocation. [tooling; zvm-cli screen model]
1. [x] DONE (zvm-cli DOS-parity, commits a74bd38..f3cabf9, pushed; plan docs/superpowers/plans/2026-06-27-zvm-cli-dos-parity.md) — CLI: zvm-cli shares the VM so all engine fixes above flow to it automatically (good headless regression harness). Optional: add a plain-text upper-window grid dump for debugging the screen model. Do NOT replicate babelmap's interactive rendering in the CLI. [tooling; zvm-cli]
   * Shipped: pinned v3 status line + v4+ upper window (ANSI scroll-region when TTY, deduped inline plain-text block when piped), --no-status byte-identical legacy output, fixed 80x24 dims, TTY terminal-BEL bleeps, raw single-key read_char, lower-window set_text_style SGR via the additive Output::print_styled seam, and cross-session aux-table persistence (<story-stem>.aux, --no-aux). Deferred sub-items remain in the PUSHED OFF list below.
   * SUPERSEDED 2026-06-27: new goal = zvm-cli should be equivalent to the old DOS interpreter (feature-complete but basic), so it DOES render a basic screen model (NOT babelmap's ratatui/automap). Two approved specs (frontend-only + one tiny additive engine method): docs/superpowers/specs/2026-06-27-zvm-cli-screen-model-design.md and -aux-persistence-design.md. Scope: pinned upper window (v4+) + v3 status line via ANSI scroll-region when interactive, deduped inline plain-text block when piped (--no-status restores byte-identical legacy output), fixed 80x24 game-visible dims, TTY-only terminal BEL for built-in bleeps, raw single-key read_char (TTY), lower-window set_text_style SGR via Output::print_styled, and cross-session aux-table persistence (<story-stem>.aux, --no-aux opt-out). To be planned + built as one parity push.

1. [x] DONE (commits d9e16e5..d24e798, pushed; spec/plan 2026-06-27-transcript-text-styles) — App: honor Z-machine set_text_style (bold/italic/reverse) in the TRANSCRIPT (main scrolling text). Per-span StyleRun list parallel to transcript; captured via print_styled; re-based across wrap; drawn per span (search-aware); persisted in transcript.json (back-compatible). Unstyled lines render/persist byte-for-byte as before. REMAINING minor: finish_resumed_turn (in-game save/restore resumed turns) still pushes empty runs. Today the app styles transcript lines only by CATEGORY (Story/Input/Meta/Warning via transcript_kinds + per-line transcript_styles override), so game-driven mid-line emphasis is dropped. Needs PER-SPAN style runs (a bold word inside a normal sentence), which ripples through: transcript data model (state.rs transcript: Vec<String> + parallel vecs), push_transcript_kind/_styled, wrap_line/wrap_lines_kinded (preserve span boundaries across wraps), the render loop (per-span Style instead of per-line), search highlighting, archive transcript.json persistence, and CaptureSink (must record style transitions by consuming the new zvm Output::print_styled seam shipped with the zvm-cli parity work). Larger feature — its own spec. [customization; app transcript rich text]
1. [~] zvm-cli: decode multi-byte terminal escape sequences (arrow/function keys) into Z-machine function-key codes for read_char. — PARTIAL (polish bundle, commit 9d6050b): arrows (129-132) + F1-F4 (133-136) decoded via decode_escape_seq; capture-once terminal restore. REMAINING: the full function-key/keypad range (other sequences fall back to raw ESC). [tooling; zvm-cli input]
1. [x] DONE (polish bundle, commit 2c7dfd4) — zvm-cli: [MORE] paging at the screen bottom (DOS-style pause-on-full-screen). TTY-only (never deadlocks piped runs), --no-more opt-out, resets after each input prompt. [tooling; zvm-cli]
1. [x] DONE (polish bundle, commit bd7d075) — zvm-cli aux store now keyed by IFID (<story-dir>/<sanitized-ifid>.aux); compute_ifid moved into zvm (app re-exports). Renaming/moving the story no longer orphans saved aux tables. [tooling; zvm-cli aux]
1. [x] DONE (polish bundle, commits 78f56b8 + 9d6050b) — zvm-cli TTY bugs reported 2026-06-27: (a) screen now clears+homes on startup so existing scrollback is not overwritten; (b) terminal echo is restored after a raw-input menu (hint screen) via capture-once original-mode restore + restore on exit. [tooling; zvm-cli screen model]

Colour Related (LostPig.z8 shakeout, 2026-06-30)

1. [x] zvm-cli showing garbage on command line when arrow keys are pressed — FIXED (commit 29c3a9a). Root cause: cooked-mode read_line_stdin, so the terminal echoed arrow/function-key escapes as literal garbage. Replaced with a raw-mode line editor (read_line_raw): crossterm parses special keys into events (consumed, no garbage); printable + Backspace + Enter handled; Ctrl-C/D exit cleanly; echo drawn in the game's current style/colour (sgr_open, shared with style_wrap) — so coloured input echo now works too; resize during input captured+applied; piped stdin falls back to cooked. PTY-verified: 0 arrow garbage, command submits cleanly.
1. [x] color on the > line "should be green" (LostPig.z8) — NOT A BUG / DROPPED. Confirmed by instrumenting set_colour/set_true_colour (zero hits) + a full raw-terminal capture: LostPig sets NO colour (only bold/reverse). The green was a Lectrote UI-theme artifact, per the user. Nothing to honor.
1. [x] zvm-cli input cursor landed in the wrong place (on the compass, inside the upper window) after a multi-row upper-window redraw (BeyondZork) — FIXED (commit c7d8acb, 2026-07-01). Model (per user): whenever the upper/lower split changes, push the lower window (and the cursor) by the changed amount. screen.rs erase() arms a pending_erase_shift; render() shifts the lower content down by the split delta (SD) and follows the cursor down (CUD), gated on erase+delta>0+top>=2 (excludes v3 1-row status + continuous flow). Portable via DECSC/DECRC + relative CUD (tmux's SD homes the cursor; xterm doesn't). Verified in a real terminal (tmux): BeyondZork first prompt at row 16 col 2; Zork1 (v3) + LostPig (v8) unchanged; 3 new unit tests. NOTE surfaced: zvm-cli's write_counted/wrap_line count SGR escape bytes as visual columns — styled text wraps at the wrong column (latent, un-filed).
1. [x] zvm-cli cursor starts at the bottom of the screen instead of at the first > (LostPig.z8) — FIXED (commit 29c3a9a). Root cause: enter_region set the scroll region AND force-parked the cursor at the bottom terminal row (\x1b[{rows};1H), so input happened at the bottom while the game's > sat mid-screen. DECSTBM already homes the cursor, so enter_region now wraps it in DECSC/DECRC (\x1b7 ... \x1b8) to preserve the cursor; input lands at the prompt. PTY-verified.
1. [x] zvm-cli (and app) HELP screen not rendering properly — FIXED (2026-06-30). Two distinct bugs: (a) OVERLAP: erase_window (LostPig HELP issues erase_window -1) was ignored by both clients, so old story bled through under the menu — fixed by honoring erase_lower_requested (commit e704e27; zvm-cli hard-clears, app records a scrollback-preserving clear boundary + top-anchors the fresh screen). (b) TOO SMALL / MISSING ITEMS: the help showed 2 of 5 items — root-caused via a differential PC trace vs Frotz (Z-code is byte-identical; both split_window(7)); the real defect was the screen model CLIPPING upper-window writes to the split height, dropping items the game draws below the split via set_cursor — fixed by growing the upper grid to cover writes below the split, bounded by header screen height 0x20, rendered in both hosts (commit 696c6f4). All 5 items (About this game / How to play IF / Special commands / Credits / Hints) now render, matching ncurses Frotz. Cursor-overwrite-on-exit is resolved by the erase clear.
1. [x] zvm-cli command-line history "should be green" (LostPig.z8) — NOT A BUG / DROPPED. Same root as the > line: LostPig sets no colour; input echo is terminal-default. Lectrote theme artifact, per the user. (If coloured input is ever wanted, the raw-mode editor above would echo in the game's current fg; the app would need to apply current_fg to the Input transcript entry — currently it uses the transcript_input theme selector.)

Slash Commands  — ALL DONE via the command-system unification (2026-06-26 subagent wave, commits fc37432..48a64c0; spec 3bfbcbf, plan c008a72). One command registry is now the single source of truth: slash commands and key bindings both flow through `slash::parse_in_context`; keys are `[keymap.context]` `"key" = "command args"` data (with `use_defaults=false` clean-slate); the `Command` enum is retired. Directional variants collapsed into parametric commands (pan-map/zoom-map/select-room/cycle-layer/nudge-room). Final whole-branch review READY TO MERGE after fixing one Critical regression (F5 reset confirmation dialog; restored via source-aware dispatch).

1. [x] Slash commands standardized in verb-noun format  — done: 48 verb-noun commands (quit/help the only one-word exceptions); old short names (save/panh/zoom/center/tidy/layer) removed (clean break).
1. [x] /help text indents on wrap  — done: hanging-indent wrap for /help + meta lines (wrap_line_hanging; continuations align under the description).
1. [x] Slash commands in categories + better help  — done: 8 categories (Game/Map/View/Transcript/Style/Export/Animation/Help); /help groups by category; `help <command>` shows one command's usage + description.
1. [x] /help lists all commands + stays in sync  — done structurally: /help enumerates the single registry, so a new command cannot be missing (the "remember to add to /help" concern is eliminated by design).

1. [x] Small-map layout collapses conflicting non-reciprocal edges onto the same cell. Repro: fresh fog-of-war map, go south 180->80, then west 80->180. South wants 80 directly below 180; west wants 80 directly right of 180 — contradictory on a grid, so with only 2 rooms the solver puts both on the same cell (renders as 180 stacked over 80). Only after exploring more rooms do the extra constraints separate them. FIX: even in the minimal conflicting case, guarantee no-overlap (separate the boxes) and pick one direction as primary, drawing the other as a distorted/bent connector — apply immediately, not only once the map grows. [automapping; mapper layout + overlap removal].  — done (wave16): subsumed by the edge/overlap tidy trigger below — layout already guarantees no-overlap + bends conflicting edges; it now re-tidies as soon as the conflicting edge lands. VERIFY in play.
1. [x] Live style editor — Phase 2.1 polish  — done (2026-06-26 subagent wave, commits 1588885..be14a65; plan fdf6c66; final whole-branch review READY TO MERGE): (a) `StyleFocus::Border` gated on `is_bordered_selector` (no phantom Border tab; StyleNav drops stale Border focus); (b) mini border-box renders the real override glyph (zone_glyph returns String, not the `•` placeholder); (c) header chip only for the 5 pane selectors, shadow chip only for `dialog`; (d) glyph-picker open is a no-op for picture-frame zones; (e) glyph-picker shows a themed `single-width only` warning hint for an invalid `pending` glyph; PLUS a ratatui scrollbar on the left selector list (overflow-only, themed via the `scrollbar` selector). [customization; render/style_editor + glyph_picker + input]
1. [x] Live style editor — Phase 2 (border & glyph editor)  — done (2026-06-26 subagent wave, commits 8b4070b..a940f33): per-element border type incl. new `rounded` + arbitrary per-side/corner glyph overrides (8 Decl glyph_* fields → PaneGlyphs on ColorScheme; render precedence override>side-style>base in both draw_pane_frame_sides and the uniform dialog path); a glyph-picker modal (curated Box-Drawing/Block-Elements/Geometric/Arrows blocks + custom codepoint range + MRU-32 + single-width validation) opened from a border box in the property pane (type cycle + 8 clickable zones + header/shadow). style.toml + recent_glyphs sidecar persistence. Final whole-branch review: READY TO MERGE. REMAINING polish → Phase 2.1 (above). [customization]
1. [x] Live style editor — Phase 1.1 polish  — done (2026-06-26 subagent wave, commits 5460bb2..51643cb): fg/bg color target + keyboard swatch nav (custom-hex/MRU can set bg); board scroll/keep-active-visible; /style alias; distinct focused-chip cursor; stricter is_valid_color_token (require #) + hardened sidecar; custom field seeds # (single-# display). [customization]
1. [x] Live style editor — Phase 1 (colors & attributes)  — done (2026-06-26 subagent wave, commits 8c194d3..4377488): full-screen click-to-edit editor (F3 / /style); preview board of every selector (click a sample to select), property pane with fg/bg swatch grid + default + custom hex + shared MRU-16 + 5 attribute toggles; edits a working StyleDoc, previews via style::resolve, saves via save_style_and_repoint (cancel leaves live theme untouched). Spec/plan in docs/superpowers. [customization]
1. [x] v5 save/restore to a memory table (vs file)  — done (2026-06-26 subagent wave, commits 6db5341..74d7f4e): the v5 EXT:0x00/0x01 table form (save/restore table bytes name) now operate on an in-memory aux_data table on Machine (bounds-clamped, no suspend; save stores 1, restore stores byte count; 0-operand forms unchanged). App persists it per a new aux_storage config (ask|archive|global, default ask): archive mode folds an aux.dat entry into the .babelmap zip; global mode keeps a per-game <ifid>.aux loaded at startup. First aux save while "ask" pops a themed prompt that writes the choice to config for all games. New: zvm aux_data/aux_dirty + opcodes; app aux_store codec+global file; archive aux.dat; config+config-screen; run-loop wiring; aux_dialog. Spec+plan in docs/superpowers. Final whole-branch review: READY TO MERGE. [engine+app]
1. [x] Support port, starboard, etc. for games that use ship directions, map to our typical directions on the map  — done (a792dd1): added nautical words to mapper parse_direction — fore/forward/bow→N, aft/stern→S, port→W, starboard→E (bow points north). No new Direction variants; offsets/opposites unaffected. Edge only recorded on room change, so stray words are harmless. [mapper; direction.rs parse_direction + test]
1. [x] Comprehensive known-titles list  — done (aaacb19 then e923715): moved the lookup to a bundled known_titles.tsv (include_str! + OnceLock HashMap, keyed by the checksum-free IFID prefix). e923715 grew it 33→127, covering every commercially-released datafile of the 35 canonical Infocom games + demos/samplers (alternate disk releases included), transcribed from An Infocom Fact Sheet rev 4.95 §V.1.2/3 with dev/beta/unreleased-final and pre-1982 v1/v2 builds excluded; cross-validated against the 33 prefixes computed from real files (all matched). So any owned copy of a stock Infocom game resolves its title in the picker + in-game banner, not just the on-hand release. v6 titles included for reference (not yet launchable). [app; session.rs known_title + known_titles.tsv]

High Priority

1. [x] Create a nice README.md — project description + major features broken down by category. KEEP UP TO DATE as features ship.  — done (13367ee): committed README; standing instruction = update it as features ship
1. [x] Need higher visibility on which window is selected  — done (8495cf3): reverse-video title on focused pane + dim unfocused content
1. [x] Fix input line (currently the > symbol is inline with game output, and duplicate > locked to the bottom of the screen)  — done (cfdd865)
1. [x] Fix missing test fixtures  — done (80024a2): minizork.z3 + praxix.z5 fetched/verified, zvm tests run with no skips
1. [x] Add configuration file for user specifics and command line args for run-time options (overrides config file if different option is select)  — done (4d19567): TOML ~/.babelmap/config.toml + clap CLI
1. [x] Add support for configurable symbols: room outlines (normal, selected, portal, current position), arrows, portal icons, paths, portal paths, etc.  — done (fe08edc): app-side SymbolSet resolved from [symbols] config (presets + per-glyph overrides), renderer reads all glyphs from it; defaults reproduce today's output. Spec/plan in docs/superpowers/
1. [x] Add sample gallery of various options for each category of symbols, support various box styles (thin, thick, borderless, etc.), use nerdfont cheat sheet to identify sets for arrows, icons, and paths, user can select preference from each category to create preferred combinations  — done (29d424c): two-pane gallery modal (g key), live preview, picks one preset per category, persists to config via toml_edit
1. [x] Current map and save file should be zipped and stored in a single file. By default a story's map should only use what is contained in this file. Today's behavior with a default map for a story should be optional. (e.g. a user may or may not want to see un-explored locations on a new run-through)  — done (3a1aca2): .babelmap is the sole save/load container (map+game); use_default_map config flag (default false = fog-of-war); legacy .map.json migrates on load
1. [x] Configurable key-map, with update hints at bottom of screen  — done (537f5b6); [keymap] TOML flat (466f042); REDESIGNED into a leader-key model: configurable prefix (Ctrl+K) opens a sticky hotkey dialog listing all commands; [hotkeys] config sets prefix/direct-set/grouping; is_direct is the uniform direct-vs-prefix rule (f76830e)
1. [x] Shift-tab should cycle between windows in backwards order  — done (be62af9): Shift+Tab (BackTab) cycles the layout backward (Split→MapFull→TranscriptFull)
1. [x] Ability to import (load) and export (save) standard saves  — done (ed169a2): export current game to a .qzl / import a foreign .qzl via the saves-manager modal (e=export, i=import) + new file-browser modal; map kept, current room re-observed; reuses persist_files save_game/restore_game
1. [x] Add '/' commands for users that prefer that syntax, eg. /panh -1 , /panv 1, /save, /load, etc.  — done (wave20, merged): leading-prefix slash commands (configurable command_prefix), curated table (/save /load /reset [map] /panh /panv /zoom /center /tidy /layer /quit /help) + every command by kebab name, quiet status-line feedback, /help to transcript (Meta), /-mode Tab autocomplete.
1. [x] Game console history should be saved and loaded (include in zip'd archive).  — done (wave24, merged): transcript.json zip entry in the .babelmap archive (lines + Story/Meta kinds), restored on every load; missing entry tolerated (empty).
1. [x] Make the map border look more like a picture frame, may composite multiple box-drawing borders to make this happen. Make the simple border a configuration option.  — done (wave18, merged): configurable border styles none/single/double/thick/picture-frame; picture-frame = notched nested corners (DEFAULT); plus a centered map LAYER-TAB strip inset in the top border (active accent, ‹…› overflow, click-rects recorded). Styled via style.toml (map_border, map_layer_tab[:active]).
1. [x] Move all style configuration settings to a separate style settings file. allow new style settings to be loaded based on the filepath. This allows users to share their styles.  — done (wave17, merged): standalone style.toml referenced from config.toml by style = "<name or path>" (inline-include base; config [colors]/[symbols] override per-key, present-keys-only); CSS-ish element->properties color selectors + symbols presets/overrides; gallery/config edits fork-write to ~/.babelmap/style.toml + repoint; gallery "Output all settings" full export. No backward compat (legacy color machinery removed). STANDING: keep style.toml schema current as new visual settings ship. share their styles.
1. [x] Make the Story border look like a book, at the top we should include the name of the adventure extracted from the file. It should be centered with nice formatting.  — done (wave18, merged): story pane border (picture-frame default) with the adventure title centered & inset in the top border; title source layered override>opening-banner>filename (style.toml story_border, story_title).
1. [x] Beautify the story header, allow it to be styled with optional box type, etc  — done (wave18, merged): status line gets optional box styling via the status_header selector (plain reversed bar by default; box when style != none).
1. [x] Beautify the input line, allow it to be styled with optional box type, etc  — done (wave18, merged): input prompt gets optional box styling via the input_line selector (plain by default; box when style != none).
1. [x] Investigate a possible hint engine.  — done (Phase 1+2, merged): in-game HINT system — /hint opens a modal mini-terminal that runs a companion Invisiclues .z5 in a SECOND Z-machine session (main game paused); discovery = remembered (per-IFID TOML index) > sibling files > inside a sibling zip; built-in-HINT suggestion (story dictionary check); zip support for adventures + hint files (load_story_bytes). Spec: specs/2026-06-24-hint-system-design.md. FOLLOW-UPS: (Phase 1 gap) AskUser manual-pick is a status stub — needs an FbMode::PickHintFile (.z3/.z5/.z8) file-browser mode; (Phase 3) online match+download of Invisiclues by IFID/title from IF Archive (consent + cache); (Phase 4) UHS reader as a 2nd HintSource (also fix the irrefutable `let HintSource::Zcode` binding then). NOTE: keymap/dead-key hint BAR is separate = #11 (done).
1. [x] Optional DROP-SHADOW style option for dialogs (configurable: on/off + shadow color/offset).  — done (wave19, merged): dialog_shadow_on flag + dialog:shadow style selector; draw_dialog paints a +1/+1 offset shadow (clamped), off by default.
1. [x] When using "reset" from the command pallete a dialog should be shown that allows the user to optionally reset the map. /reset command should support as an option.  — done (wave22, merged): reset confirmation dialog (shared chrome) with an opt-in "Also clear the map" checkbox (map kept by default), Esc/[X] cancel; /reset and /reset map act immediately via a shared reset_game helper.
1. [x] Hide room numbers by default, when hidden move portal icons to bottom row of room, create command to turn room number on/off.  — done (wave22, merged): show_room_numbers config (default false) + toggle-room-numbers command; #id hidden by default, portal icons relocate to the freed bottom row.
1. [x] Ability to SEARCH the console/transcript output (find + highlight/jump to matches), and EXPORT the console to a plain text file. ALSO: FILTER the transcript by category — only STORY (game output), only META (slash-command output, /help, app messages), or BOTH. Requires tagging each transcript entry Story|Meta.  — done (wave23, merged): /search (case-insensitive, backward-default, n/N nav, configurable [search] keys, UTF-8-safe highlight), /filter story|meta|both + indicator, /export auto-path honoring filter; shared visible_transcript_indices view; + themeable ▏ meta gutter marker. FOLLOW-UPS (minor): (M1) /filter does not re-scope an active search until the next /search (stale [i/N] hint); (M2) /search collapses runs of internal whitespace (split+join); (M3) scroll_for_match is logical-line based so a match can land just off-screen when preceding lines wrap.

Medium Priority

1. [x] Add more steps in animation for various layout tasks and cleanup tasks to help diagnose, describe which algorithm is running in each part of the step  — done (4b4e819): relayout broken into 5 stage frames + per-move cleanup frames (each described, e.g. "moved 180 to clear overlap with 193"); TidyFrame carries description/stats/stage_start; playback panel shows algorithm+stats; Ctrl+arrows jump between stages
1. [x] Add autocomplete and suggestions for common verbs and items found in room description (can we peak into story file?)  — done (4702355 + 254c65d): Tab-completion from story dictionary + room nouns, live suggestion line
1. [x] Add menu based input selection for common verbs and items  — done (f073c19): two-pane verbs|nouns token palette (m key), picks append tokens to input (multi-noun via prepositions). Note: inventory nouns come from parsed-i fallback; live list_inventory is a future refinement
1. [x] Add mouse support with clickable rooms that display extended room information (i.e. room inspector), left click shows game/story related information, right click shows diagnostics  — done (b503ccf): left-click room-info panel (name/notes/exits + objects for current room), right-click diagnostics (reuses inspector); middle-drag grab-pan; mouse capture enabled/released
1. [x] Add mouse wheel support for map scroll  — done (b503ccf): wheel pans the map (Shift=horizontal, Ctrl=zoom), and scrolls the transcript over the story pane
1. [x] Add support for multiple save files  — done (b83e6f8): named .babelmap slots, saves-manager modal (Ctrl+O) with load/save-as/delete, Meta carries name/turns/timestamp
1. [x] Configurable user directory  — done (4d19567): user_dir in config.toml + --user-dir CLI flag
1. [x] Optional auto-save (on each action) and auto-load with story start  — done (0f3e9c0): auto_save (per-turn archive write) + auto_load (default on; gates restore_quetzal, map always loads) config flags
1. [x] Reset the game and start over (requires user confirmation)  — done (fceb7e1): ResetGame (F5 / hotkey dialog) → confirm prompt → rebuilds session from story bytes, keeps the map, resets turns/transcript
1. [x] Configuration screen for all options, command-line options (command-line takes precedent over config file)  — done (merged wave14, 5c3ea27): F2 opens a scalars/enum/preset settings modal; explicit Save (format-preserving write_config + live re-resolve of symbols/colors) + Cancel; runtime Config moved into AppState. FOLLOW-UPS: (cli) read-only marker for CLI-overridden user_dir not yet shown; colors.scheme row cycles presets but Enter doesn't open a custom-path text-edit; record_history row is a no-op until rewind/replay wires it
1. [x] Help screen  — SUPERSEDED by the leader-key hotkey dialog (f76830e): the prefix-activated dialog lists all commands grouped/ordered, serving the help-reference purpose; the standalone F1 overlay was removed

Low Priority

1. [x] Beautify UI (color schemes, fancy console, etc)  — done (1612736): [colors] scheme = a Ghostty theme file or built-in (mono/high-contrast/tomorrow-night) recolors rooms/connectors/states + chrome; per-element [colors.elements] overrides; default = terminal colors. (colors module a6a22f0 + render wiring 1612736)
1. [x] Hide able inventory panel  — done (1366eb3): toggleable story-pane strip (v / hotkey dialog) of carried items; heuristic player-object detection (object whose parent follows the room) + parse-of-inventory-output fallback
1. [x] Optional background map updates (background tidy on exploration)  — done (0f3e9c0): background_tidy config (Off/EveryRoom/OnOverlap/Debounced, default EveryRoom) auto re-tidies the active layer as rooms are discovered
1. [x] Game state rewind / replay / resume — DONE (merged wave-rewind-replay): opt-in record_turn_history (default false) records per-turn Quetzal+map-snapshot-on-change+transcript into the .babelmap (v2 archive; v1 loads empty); F4 replay modal steps/auto-plays with a reconstructed map preview; linear resume restores VM+map and truncates later history (clears undo stack). FOLLOW-UPS below. [app]
1. [x] In-game (game-initiated) save/restore (v4+) — DONE (merged step B): the game's own SAVE/RESTORE now flow through the saves dialog; SAVE writes a .babelmap (map bundled), RESTORE reads .babelmap(+map)/.qzl; complete_restore_success stores 2 at mem[pc-1] so the original @save returns 2 and the game REDRAWS its status line (verified on Bureaucracy: upper window rows 0->1). v3 keeps the host-mediated "isn't wired" message. [app+zvm]
1. [x] Persist Z-machine screen state in saves (step A) — for the SNAPSHOT path (Ctrl+R / auto-load at launch).  — done: ScreenDto + screen.json in the .babelmap (zvm stays dep-free; app-side DTO), ArchiveContents.screen, re-applied at the 4 host-mediated restore sites; ScreenState/UpperWindow gained Clone; round-trip test screen_state_round_trips_through_archive; verified rows 0->1 on bureaucr.z4. FOLLOW-UP done (20606d3): the auto_load-off launch-resume-DIALOG path now threads ac.screen through pending_resume_stash/state.pending_resume and apply_launch_resume re-applies it. [app; archive + restore sites + zvm screen serde]
   Also done this session (save/restore UX): saved transcript filtered to Story+Input only (drops Meta/Warning); in-game restore prints [Game restored from NAME] matching Ctrl+R; save/export prompt restyled to the story input line with "Save Filename:"/"Export Filename:" labels.

Feedback Tweaks (2026-06-24 feature review) DONE

1. [x] Background tidy should also fire when an EDGE is added/updated, not only when a new room is discovered. Currently background_tidy keys off new-room discovery, so a second edge on an already-known pair (e.g. west 80->180 after south 180->80) never triggers a relayout — the overlap above persists until an unrelated new room forces a tidy. FIX: trigger background_tidy on edge add/update too, respecting the existing modes (Off / EveryRoom / OnOverlap / Debounced). [automapping; app run loop + background_tidy trigger]  — done (wave16, 79a3b65): overlap/distortion signal computed every turn and fed to all modes; EveryRoom fires on new_room||overlap, Debounced fires immediately on overlap
1. [x] Middle-button pan is jumpy — it moves a whole grid cell (room column/row) per step because map scroll is tracked in grid/room units. Make panning move character-by-character (sub-cell). FIX: track a sub-cell pan offset in terminal characters and apply it in the renderer off_x/off_y; mirror the same offset in the mouse hit-test (room_screen_rects) so clicks stay accurate. Consider applying the same finer granularity to wheel-pan. [navigation; render/map.rs scroll model + mouse hit-test]  — done (wave15, 1cf327b): char_pan offset, drag accumulates at 1-char precision, hit-test mirrored
1. [x] Mouse zoom (Ctrl+wheel) is too sensitive — each tick jumps too far, so the comfortable middle zoom level is unreachable (always overshoot). FIX: smaller per-tick zoom increments (more, finer zoom steps), and add a reset-zoom-to-default binding so the middle/default level is always one keypress away. [navigation; render/map.rs zoom step + keymap]  — done (wave15, 2e85ddb): zoom_level 0-8 (default 7), ZoomReset bound to plain 0 (also clears char_pan)
1. [x] No visual indication when the CURRENT room is the selected room — the current-position styling masks/overrides the selection highlight, so selecting the room you're standing in looks no different. FIX: make selection visually distinct even on the current room (combine the selected highlight with the current-position marker, or a dedicated current+selected style). [navigation; render/map.rs room styling — current vs selected]  — done (wave15, 8e992ee): current+selected uses selection color + REVERSED, heavy outline kept
1. [x] On the currently selected room, the exit arrows are mis-colored: the arrow BACKGROUND should match the room's color, and the arrow glyph (foreground) should be the path color. Currently the selection styling overrides these. FIX: when rendering arrows for the selected room, set bg = room color and fg = path/connector color (don't let the selection highlight repaint them). [navigation; render/map.rs arrow coloring under selection]  — done (wave15, d745dc6): selected-room arrows painted bg = colors.room_selected.bg, fg = connector color (non-selected arrows unchanged)
1. [x] Center does not actually center — content lands at the TOP of the map pane instead of the middle (seen clearly with only two rooms). Likely the centering math ignores vertical extent / pane height (or centers on cell origin). FIX: center the map content (or current room) in the viewport on BOTH axes, accounting for the map bounding box and pane height; verify with the minimal 2-room case. [navigation; render/map.rs center/scroll calc]  — done (wave15, 5dd660a): recenter_on uses the real Boxes cell footprint (13x7) not the stride (19x11); centers both axes, clears char_pan; verified on 2-room map
1. [x] Map-focus hint bar dead keys — DONE (already fixed; verified + guarded): hint_bar filters on is_direct + key-resolves-in-context, so dialog-only commands (gallery/inspect/layout) are never advertised; Map bar renders only "Tab: focus | +: zoom in | C: center | N: next room". Added a regression test asserting their exclusion, and removed the tidy hint (AnimateTidy) from GAME_HINTS. [navigation/UX; hint-bar generation from keymap + is_direct]
1. [x] Map pulses (tidy) on a non-mutating command like "look" — DONE: the bg-tidy gate keyed on `overlap` (a persistent state predicate), so a lingering layout overlap re-fired a tidy every turn. should_bg_tidy now takes a `changed` flag (room+conn counts before/after the turn) and won't tidy when the graph is unchanged. [map/UX; input.rs should_bg_tidy + main.rs run loop]
1. [x] Add a scrollbar to the story/transcript panel showing scroll position + extent (ratatui Scrollbar widget).  — done (38dd3c2): vertical Scrollbar in a reserved rightmost gutter column of the transcript body, shown only when wrapped content overflows; visible_wrapped_lines_kinded returns total rows for sizing. Wheel direction fixed + thumb accuracy (700e2c5), themeable via the `scrollbar` selector (2a69ba9). [story; render/transcript.rs]
1. [x] All dialogs and docked frames should have a little clickable X in the corner to close them (mouse click = close).  — done (wave19, merged): all 9 modals render via the shared render/dialog.rs chrome with a clickable [X] + dialog-aware mouse_to_action; ESC==[X] uniform. (Note: the inventory STRIP isn't a dialog so has no X; verb menu is now a centered dialog with one.)
1. [x] Default keybindings for commands in the command/hotkey dialog should be plain keys — none should require CTRL or SHIFT.  — done (wave24, merged): scope = direct commands only; Ctrl+arrows nudge → F6-F9, redundant Shift+arrow pan & Shift++ zoom removed (plain arrows+hjkl+/= remain in Map/Anim); prefix Ctrl+K, save/restore Ctrl+S/R, quit Ctrl+Q/C, and Shift+Tab (BackTab) intentionally kept; invariant test direct_default_bindings_have_no_modifiers.
1. [x] Current-room color bleeds through into the command/hotkey dialog.  — done (wave19, merged): the shared draw_dialog fills the full rect opaque (Style::reset().patch(frame)); bleed test confirms the underlying REVERSED/bg is cleared.
1. [x] Enhance the symbol gallery with many more options.  — done (wave21, merged): nf-md arrow families (bold/box/circle/outline) + corner arrows, distinct 4-icon nerdfont-stairs portal preset, higher-fidelity preview (real box + 8 arrows + longer path + 4 portals); + later solid & super-thick block box styles, tabbed gallery layout (categories on top ←→, options ↑↓), arrows hidden on Box page, nerdfont N/S fix.
1. [x] Redesign the gallery screen — border, title bar, centered dialog; fix opacity/cursor leaks.  — done (wave19, merged): gallery is now a centered bordered dialog via the shared chrome ([X]+[Done], opaque bg); story cursor suppressed while any overlay is open (any_overlay_open).
1. [x] Do NOT dim the MAP pane when it is unfocused (keep it full-brightness always), but DO keep dimming the STORY pane when it is not in focus. Currently both panes dim when unfocused (8495cf3). Make the dim-on-unfocus apply to the story pane only. [navigation; render focus/dim logic]  — done (wave15, af6490b): removed the dim-map branch in draw_frame; only the story/transcript pane dims when unfocused
1. [x] Prompt save-on-quit / load-on-launch when auto-save/load off — DONE (already implemented): config prompt_save_on_quit (default true) + should_prompt_save_on_quit + the QuitDialog (Save/Quit/Cancel) at Action::Quit; config prompt_load_on_launch (default true) + pending_resume_stash + the launch resume dialog. [app; config.rs + main.rs quit/launch]
1. [x] Config options for auto-load/auto-save + prompt toggles — DONE (already implemented): config.rs auto_load, auto_save, prompt_save_on_quit, prompt_load_on_launch (all in the file-merge + write_config). [app; config.rs]
1. [x] FOLLOW-UP (wave25): the unconditional save-on-exit overrode "Quit without saving".  — done (merged-pending): exit-save now gated by auto_save; with auto_save off (default) NOTHING is saved automatically. DECIDED MODEL: the map is saved inside the .babelmap zip ONLY when the game state is saved (no separate auto map save).
1. [x] /help doesn't list all slash commands.  — done (30f3148): help_text now enumerates the ALL_COMMANDS kebab names (deduped against CURATED, same union slash_names() builds) in a "Keymap commands (also available by kebab name):" section, each with its Command::label(); regression test help_text_lists_keymap_kebab_commands. New keymap commands now appear automatically. [app/UX; slash.rs help_text + slash_names]
1. [x] Allow mouse selection and copying text  — done (4ffd3be): left-drag in the story pane selects text (reversed highlight), release copies via OSC 52 (no dep, works over SSH). Selection clamped to the story column band (won't grab the map); scrollbar gutter excluded (d1854dd); copy reads the rendered frame buffer (a61b431). New dependency-free clipboard module (base64 + OSC52 + column-clamped extract) with unit tests. [app; clipboard.rs + input/render wiring]
1. [x] Set mousewheel scroll direction (should match current terminal by default)  — done (e6c99ba): mouse_wheel_invert config flag (default false = conventional); mouse_to_action swaps wheel up/down for map + transcript when set.

Others DONE

1. [x] Live style editor — Phase 2.2 (save/load model + property-pane polish)  — done (2026-06-27 SDD wave, commits 2f83b61..235add0; spec 29cb3c4, plan 691c9a1; final whole-branch review READY TO MERGE). Five items: (a) editor now OPENS over the active merged style (global + per-game styles/<ifid>.toml), not global-only; (b) single Save replaced by "Save Global Style" + "Save Game Style" buttons (g key) — Save Game writes the FULL self-contained look to styles/<ifid>.toml via write_style_full, no config.style repoint; create-game-style command + Action::GameStyle removed; (c) fg/bg target indicator (▸ marker on the active label + ` hex →fg `/` hex →bg ` tag) on the shared custom/MRU region; (d) obvious bracketed hex edit box `[ #ab12cd▏ ]`; (e) stable color MRU (append-to-end + keep-position + ring-evict, no more positional cycling). 802 tests green, 0 warnings. FOLLOW-UP tracked above (per-game "default" fields not truly frozen). [customization; input/main/styles/style_mru + render/style_editor + render/dialog]
1. [x] Test isolation: style-editor unit tests inherited the real `~/.babelmap/style.toml` via `ed.doc` (open_style_editor → load_style over `AppState::default()`'s `~/.babelmap` user_dir), so a contributor whose on-disk config set an asserted selector got spurious failures (and `StyleSave`/`save_mru` wrote into the real dir during tests).  — done: added `#[cfg(test)] open_style_editor_hermetic` (input.rs) that rebinds `config.user_dir` to a fresh, unique, empty temp dir per call before opening → editor always reads the built-in default style; converted all 30 style-editor test call sites (input.rs + render/style_editor.rs + render/glyph_picker.rs) to it; removed the now-redundant fg/bg neutralizer and the manual empty-dir hack; kept the glyph-picker `.style = None` patches (the BUILT-IN default also sets map_border=picture-frame, so they guard the default, not just disk). New regression test `hermetic_editor_ignores_ambient_user_dir` writes a poison style.toml and asserts the helper ignores it. 793 tests green, 0 warnings. [test-infra; input.rs/render style-editor tests]
1. [x] Engine-wave review minors (2026-06-25): (a) custom alphabet table not honoured on the ENCODE side — done (524731f): added encode_word_mem(text, mem) reading the 78-byte custom table (v5+ header 0x34); dictionary lookup + encode_text opcode use it, encode_word (defaults) kept for the rest; round-trip + no-table tests; (b) verify zero-file-length guard — RESOLVED not-a-bug (2026-06-25): file_length==0 with ck!=0 only arises from a malformed header, and branching "not genuine" for a corrupt header is acceptable; real games carry a correct file_length. [engine; zvm]
1. [x] Rewind/replay review minors (2026-06-25): (a) replay preview falls back to the LIVE map for turns before the first map snapshot — done (80d7d05): replay_graph now yields an empty map (never the live graph) when no snapshot at-or-before the turn; (b) the modal doesn't surface the selected turn's transcript text — done (1016e21): the replay modal now shows the selected turn's transcript under the turn list; (c) per-turn capture recorded one junk final turn on quit — done (80d7d05): capture guarded with !result.quit. [app; render/history + main.rs capture]
1. [x] Transcript over-scroll blanked viewport rows from the bottom up — done (c9aa195): visible_wrapped_lines_kinded clamps scroll to n-rows; render reports max_scroll up to draw_frame which clamps state.transcript_scroll after each draw. [story; render/transcript.rs + main.rs run loop]

Z-machine engine coverage (2026-06-25 audit) DONE

1. [x] [PASS] scan_table (VAR:0x17) — DONE (exec.rs:884 arm + decode.rs:205 stores AND branches; tests scan_table_word_finds_and_stores_address /_not_found_stores_zero /_byte_form_compares_low_byte). [engine; zvm exec_var]
1. [x] [PASS] copy_table (VAR:0x1D) — DONE (exec.rs:906 arm; tests copy_table_copies_forward / _zeroes_when_second_is_zero /_positive_size_overlap_is_noncorrupting). [engine; zvm exec_var]
1. [x] [PASS] print_table (VAR:0x1E) — DONE (exec.rs:932 arm; test print_table_emits_each_row_chars). [engine; zvm exec_var]
1. [x] [PASS] get_cursor (VAR:0x10) — DONE (exec.rs:877 arm; test get_cursor_writes_row_and_col). [engine/screen; zvm exec_var]
1. [x] [PASS] erase_line (VAR:0x0E) — DONE (exec.rs:954 arm; tests erase_line_is_recognized_noop_without_warning /_clears_to_end_of_row_in_upper). [engine/screen; zvm exec_var]
1. [x] [PASS] Silent VAR fallthrough now warns — exec.rs:989 records each unimplemented VAR opcode once (warned_var_opcodes set → diagnostics "unimplemented VAR opcode 0x.. (ignored)"); surfaced in the transcript as Warning lines. [engine; zvm exec_var]
1. [x] UNDO — DONE (merged #49): in-memory multi-level undo reusing Quetzal (Machine.undo_stack of (blob, save_undo store target) + undo_cap); save_undo stores 1 / -1 when disabled, restore_undo restores + stores 2 into the original target, inline (no host suspend); depth = config undo_levels (default 16, 0 disables); file restore clears the stack. [engine; zvm exec_ext]
1. [x] tokenise (VAR:0x1B) — DONE (merged engine wave): reuses read_text_buffer + dictionary tokenise + shared write_parse_buffer. Custom (non-zero) dictionary operand not honoured (uses the standard dict) — rare; TODO comment in code. [engine; zvm exec_var]
1. [x] encode_text (VAR:0x1C) — DONE (merged engine wave): encodes length ZSCII bytes at text+from to the packed dict form via encode_word. [engine; zvm exec_var]
1. [x] print_unicode (EXT:0x0B) / check_unicode (EXT:0x0C) — DONE (merged engine wave): print outputs the codepoint (invalid -> U+FFFD); check stores 3 for a valid scalar, 0 otherwise. [engine; zvm exec_ext]
1. [x] set_colour (2OP:0x1B) — DONE (merged engine wave): explicit graceful no-op (babelmap styling owns the look). [engine; zvm exec_2op]
1. [x] set_true_colour (EXT:0x05) — DONE (merged engine wave): explicit graceful no-op. [engine; zvm exec_ext]
1. [x] set_font (EXT:0x04) — DONE (merged engine wave): reports the fixed font (font 1/0 -> 1, else 0). [engine; zvm exec_ext]
1. [x] Custom alphabet table (header 0x34, v5+) — DONE for DECODE (merged engine wave): glyph rows come from the table when present (A2 escape/newline kept built-in). ENCODE side done too (524731f): encode_word_mem reads the table; dictionary lookup + encode_text use it. [engine; zvm text/decode + encode]
1. [x] verify (0OP:0x0D) / piracy (0OP:0x0F) — DONE (merged engine wave): verify checksums the ORIGINAL story image (original_dynamic below static base + live mem above) vs header 0x1C and branches; piracy always branches (genuine). [engine; zvm exec_0op]

UI / app DONE (2026-06-25 session)

1. [x] Allow story text to be styled — DONE (transcript text styling by category: Story/Input/Meta/Warning + built-in location/system + user [[transcript.rule]] regex rules; per-category fg/bg/bold/italic + gutters; merged #75).
1. [x] Stylize score bar — DONE (configurable [statusbar] segment bar: text templates w/ {placeholders}, left/center/right clusters, per-segment style; merged #76).
1. [x] Stylize text input, '>' character, allow border (top/bottom/left/right), text color, auto-complete color — DONE: input-line per-side borders (#82); transcript:input + suggestion selectors; live input text + '>' prompt now separately styleable via input:text / input:prompt selectors (a75af60). Autocomplete color slot = the existing `suggestion` selector.
1. [x] Bureaucracy blood-pressure/upper-window box too big — DONE: root cause was us (upper window stretched to pane width, border at pane edge, while the game screen is upper.cols wide and games center content within it). Fixed by rendering the upper window at game-screen width (+ side borders) centered in the pane; left-align+scroll when pane narrower. [render/upper_window.rs]
1. [x] Inventory panel not updating on drop/acquire — DONE: root cause was the fragile movement heuristic to lock player_obj; until it locked, the panel used the stale parse-fallback (only refreshed by an explicit inventory cmd). Now lock player_obj via zvm::find_player_object (name-based: "you"/"yourself"/…; minizork player=#30) first, heuristic as fallback. Verified end-to-end (take leaflet->["leaflet"], drop->[]). [app; main.rs inventory tracking + zvm location]
1. [x] zvm object_tree_view panics (index out of bounds, memory.rs:90) on the czech.z5 torture fixture.  — done (921b847): max_object_number now stops when an entry would extend past EOF or its property-table pointer lands outside the file; regression test object_scan_eof. [engine; zvm objects/location object scan]
1. [x] Story-title heuristic weak — DONE: resolve_title is now override > known-title table > banner anchor heuristic > filename. The anchor heuristic takes the line above the first Infocom boilerplate line (copyright / interactive fiction|fantasy / Serial number / trademark), avoiding copyright/epigraph/narration false-positives; the table (keyed by IFID release+serial prefix, checksum-independent) is seeded with the 8 ./stories games verified from their files. [app; session.rs resolve_title/title_from_banner/known_title]
1. [x] Allow styling to specify each border (top/bottom/left/right) independently — DONE (per-side style_top/bottom/left/right overrides + header decoupling; merged #82).
1. [x] Show the selected turn's transcript in the replay modal — done (1016e21). [app; render/history.rs]
1. [x] Make the transcript scrollbar themeable — done (2a69ba9): `scrollbar` style selector (track + thumb) via style.toml. [app; colors.rs + style.rs + render/transcript.rs]
1. [x] Optional load screen listing available story files with pretty titles + filenames; launches when a directory is passed instead of a story file.  — done (54433e6): full-screen story picker (new picker.rs scan_stories: top-level scan, skips v6/unsupported, title via known_title else filename stem, sorted); keyboard + mouse nav, themed via dialog selectors. Titles upgraded by the bundled known_titles.tsv (8→33). [app; picker.rs + main.rs picker loop]
1. [x] Bureaucracy (v4+) status bar was garbage (v3 globals read as location/score/moves) — done (609e84f): v4+ now shows a babelmap-owned line (detected room + turn + detection method + filter); v3 keeps its real status line. Hidable for v4+ only via show_status_bar config + ToggleStatusBar (hotkey/slash); v3 always shows; hidden row collapses but pops up for transient messages. [app; render/transcript.rs status bar + config/state/keymap/input]
1. [x] [TODO-91678e] zvm-cli at 40-col width: on BeyondZork's main play screen the boxed room description overflows the box; the game shows its own "[MORE][Press DOWN arrow to scroll]" prompt, but pressing DOWN does not scroll. INVESTIGATE whether the DOWN arrow reaches the game. Note: zvm-cli's OWN pager prints only "[MORE]" (main.rs:98), so the "[Press DOWN arrow to scroll]" text is BeyondZork's — the game is waiting on a key. HYPOTHESES: (a) LINE INPUT swallows it — if the scroll prompt is a `read` (line input), zvm-cli's raw line editor CONSUMES arrow keys (main.rs:435 "Arrows... are consumed") so DOWN never terminates/reaches the game. This overlaps the audit follow-up "terminating-characters table (0x2E) unwired" (a v5 game installs a term-chars table so function keys like DOWN=130 end line input; zvm-cli hardcodes Enter). (b) READ_CHAR path already delivers it — decode_keycode(Down)=130 → supply_char (main.rs:292/681-687), so if it's a read_char the key IS sent; then the non-scroll is a screen-model issue (upper-window box clipped/too small at 40 cols, growth bounded by header screen height 0x20). FIRST STEP: determine whether BeyondZork's box-scroll uses read vs read_char here, then fix accordingly. [tooling; zvm-cli input path + terminating-chars wiring] — completed 2026-07-01
1. [x] [TODO-62930b]    * [ ] (audit follow-up, tiny) `check_unicode` (EXT:0x0C, exec.rs:1265) over-reports input: returns 3 (print+input) but the input path is u8-limited (`supply_char(ch: u8)` exec.rs:1690, no ZSCII mapping). Report 1 (print-only) until real Unicode input lands. [engine; zvm exec.rs check_unicode] — completed 2026-07-01
1. [x] [TODO-964e7f]    * [ ] (audit follow-up, tiny) EXT unknown-opcode falls through to a SILENT `_ => Continue` (exec.rs:1293) — mirror the VAR one-time diagnostic (exec.rs:1130) for observability parity. [engine; zvm exec.rs exec_ext] — completed 2026-07-01
