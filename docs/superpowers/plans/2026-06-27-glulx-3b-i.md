# Glulx 3b-i (Engine Abstraction) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Route the app to the game through an engine-neutral `Engine` trait + neutral types (`KeyInput`, `ScreenModel`, `Introspect`, reserved `Debugger`, `EngineSave`), with `zvm`'s `GameSession` implementing it. **Zero behavior change.**

**Spec:** `docs/superpowers/specs/2026-06-27-glulx-3b-i-engine-abstraction-design.md`

## Existing integration sites (route these through the trait)

- `main.rs`: `session: GameSession` (param ~252), constructed ~943; render `draw_upper_window(&session.machine, …)` (344, 415) + `render_transcript(&session.machine, state, …)` (346, 417); dict `zvm::dictionary::load(&session.machine.mem)` (1068); scope/inventory via `zvm::object_tree_view`/`find_player_object`/`objects::get_parent`/`inventory::*` (3337–3382); save/restore `session.machine.restore_file`/`save_quetzal`/`screen` (993–998 etc.); header `&mut session.machine.mem` (962); misc `&session.machine.mem` (507); char input `key_to_zscii(KeyEvent)` → `submit_char` (1917, fn at 3993).
- `state.rs`: `HintSource::Zcode(GameSession)` / hint sessions — these stay concrete `GameSession` (the Invisiclues companion is always Z-machine; not part of the trait). `dict_words` field (849).
- `session.rs`: `GameSession`, `TurnResult`, `apply_turn`.

## Global Constraints

- **ZERO behavior change.** Full `cargo test -p app` and `cargo test --workspace` green after every task; Z-machine rendered output byte-identical.
- 0 warnings (`cargo build`, `cargo doc -p app --no-deps`).
- Neutral types are app-owned and engine-agnostic (no Glk/Glulx/Z-machine specifics in the trait surface).
- Commit-only on the phase's worktree branch; one commit per task (TDD). No push. Do not edit `TODO.md`.
- Commit trailers, every commit (no backticks in the body):
  `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
  `Claude-Session: https://claude.ai/code/session_01Uvf2RNUS7SBZHXPWqcRAkV`

---

## Task 1: Neutral types + the `Engine` trait (definitions)

**Files:** new `crates/app/src/engine/mod.rs` (or `engine.rs`) — `Engine` trait, `KeyInput`, `ScreenModel`/`WinNode`/`GridWindow`/`BufferWindow`/`Split`, `Introspect`, `Debugger` (reserved), `EngineSave`, `EngineError`; re-use existing `InputKind`, `TurnResult`, `LocationInfo`.

- [ ] **Step 1: Failing tests:** `KeyInput` maps from a few crossterm `KeyEvent`s (a `key_event_to_input` helper); `ScreenModel` constructs a 2-node tree; `EngineSave` round-trips its tag. (Pure type/helper tests.)
- [ ] **Step 2: Define** the trait + types per the spec (default `introspect`/`debugger` → `None`). `Debugger` is declared but unimplemented.
- [ ] **Step 3: Commit** — `feat(app): engine-neutral Engine trait + KeyInput/ScreenModel/Introspect types`.

---

## Task 2: `GameSession` implements `Engine` (the zvm adapter)

**Files:** `crates/app/src/session.rs` (+ a `session/zvm_engine.rs` if cleaner).

- [ ] **Step 1: Failing tests:**
  - `screen()` for a **v3** machine yields a 1-row `Grid` whose cells equal `compute_status_line()` (location + score/moves), over a `Buffer`.
  - `screen()` for a **v4+** machine yields a `Grid` mirroring `screen.upper` with the correct logical size + cursor, over a `Buffer`.
  - `submit_key(KeyInput::Up)` produces the same ZSCII byte the relocated `key_to_zscii` would (parametrize a few keys).
  - `introspect()` returns vocabulary == today's `dictionary::load(...).words(...)`, and `inventory()`/`scope_nouns()` match the existing logic on a fixture game.
  - `save_state()`/`restore_state()` round-trip; `EngineSave.engine == "zmachine"`.
- [ ] **Step 2: Implement** `impl Engine for GameSession`: the turn methods (delegate; `submit_key` runs the relocated `key_to_zscii`), `screen()` (the v3/v4+ adapter — synthesize the v3 status grid; expose the v4+ upper grid + logical size/viewport), `save_state`/`restore_state` (wrap Quetzal + tag), `current_location`, `as_any`, and an `Introspect` impl wrapping the existing dict/object-tree/inventory/room logic (move the read helpers here or call them).
- [ ] **Step 3: Commit** — `feat(app): GameSession implements Engine (v3/v4+ screen adapter + Introspect)`.

---

## Task 3: App holds `Box<dyn Engine>`; route the turn cycle + input

**Files:** `crates/app/src/main.rs`, `state.rs`.

- [ ] **Step 1:** Change the app's session storage to `Box<dyn Engine>` (keep `HintSource::Zcode(GameSession)` concrete — hints are separate). Construct it from `GameSession::new(...)` boxed.
- [ ] **Step 2:** Route the turn cycle through the trait: `submit`, `pending_input`, `resume_save`/`resume_restore`, `take_transcript`. Replace char input: `KeyEvent → KeyInput → engine.submit_key(...)` (remove the app-level `key_to_zscii` call site; the conversion now lives in the zvm impl). Header seeding that used `&mut session.machine.mem` (962) moves behind a small `Engine` hook or the boxed concrete construction (a `GameSession`-specific setup before boxing is fine).
- [ ] **Step 3:** Run the full suite — turn cycle, input, history, auto-save behave identically.
- [ ] **Step 4: Commit** — `refactor(app): drive the turn cycle and input through Box<dyn Engine>`.

---

## Task 4: Render from `ScreenModel` (byte-identical)

**Files:** `crates/app/src/main.rs`, `crates/app/src/render/upper_window.rs`, `render/transcript.rs`.

- [ ] **Step 1:** Feed rendering from `engine.screen()`: `draw_upper_window` takes the `Grid` node (logical size + viewport + cells) instead of `&Machine`; the v3 status path and the v4+ upper path collapse into the one `Grid` render. `render_transcript` keeps the app transcript; any machine read it did (e.g. v3 status) comes from the `Grid` node now.
- [ ] **Step 2: Equivalence tests:** for a v3 and a v4+ fixture, the rendered `Buffer` (cells) from the new path equals the previous output (snapshot/compare); the existing render tests pass unchanged.
- [ ] **Step 3: Commit** — `refactor(app): render the screen from the neutral ScreenModel (v3/v4+ collapsed, output-identical)`.

---

## Task 5: Play-aids + save/load through the trait

**Files:** `crates/app/src/main.rs`, `crates/app/src/archive.rs`, `state.rs`.

- [ ] **Step 1:** Autocomplete (`dict_words`), verb/noun menu (`scope_nouns`), inventory strip (`inventory`), and room/location reads go through `engine.introspect()` / `engine.current_location()` (the data is identical — same underlying logic, now behind the capability). Where a site held `&session.machine.mem` for object/dict reads, it uses the introspect surface.
- [ ] **Step 2:** Save/load through `engine.save_state()`/`restore_state()`; the `.babelmap` archive records the `EngineSave` tag and **refuses a mismatched-engine restore** (a new guard — for now only `"zmachine"` exists, so it always matches; the guard is exercised by a unit test with a faked foreign tag).
- [ ] **Step 3:** Full suite green — autocomplete / verb menu / inventory / save-load / location detection all behave exactly as before.
- [ ] **Step 4: Commit** — `refactor(app): play-aids + save/load through Engine introspect/save_state (engine-tagged)`.

---

## Self-review checklist (run before final review)

- Nothing in the app reaches `session.machine.*` for the main game anymore (hints excepted); everything goes through `Engine`/`Introspect`.
- The trait surface is engine-neutral (no Glk/Glulx/Z-machine types leak in).
- v3 and v4+ render byte-identical; the two screen paths are collapsed into one `Grid` render.
- Autocomplete / verb menu / inventory / save-load / location detection unchanged (their tests pass untouched).
- `debugger()` returns `None` everywhere (reserved); `EngineSave` is tagged and the mismatch guard works.
- 0 warnings; full `cargo test --workspace` green (no behavior change).
