# Glulx 3b-ii (Glulx Plays in the TUI) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Play Glulx (`.ulx`/`.gblorb`) games in babelmap: a `GlulxSession` implementing `Engine`, an app `GlkBackend` mapping Glk windows onto the neutral `ScreenModel`, a generic multi-window renderer (map alongside), Glk input, and `.gblorb`/`.ulx` routing. Z-machine behavior stays byte-identical.

**Spec:** `docs/superpowers/specs/2026-06-27-glulx-3b-ii-glulx-plays-design.md`

## Real interfaces (from 3b-i + gvm)

- `crates/app/src/engine.rs`: `trait Engine` (`submit`, `submit_key(KeyInput) -> Option<TurnResult>`, `take_transcript`, `pending_input`, `resume_*`, `screen() -> ScreenModel` *by value*, `save_state`/`restore_state`, `current_location`, `as_any*`, `introspect`/`debugger` default `None`); `KeyInput` + `key_event_to_input(KeyEvent) -> Option<KeyInput>`; `GridWindow`/`GridCell`; **`BufferWindow {}` (currently empty — extend for Glulx)**; `WinNode { Pair{vertical,split,first,second}, Grid, Buffer, Blank }`; `ScreenModel { root, status: StatusModel }` + `ScreenModel::grid()`; `StatusModel`/`StatusField`; `EngineSave::new(engine, fmt, bytes)`.
- `crates/gvm`: `Machine::with_glk(mem, Box<dyn GlkBackend>)`, `StepResult::{Continue, Quit, NeedLine{..}, NeedChar{..}}`, `supply_line`/`supply_char`; `glk::GlkBackend` (window_open/close/layout, put_text(win, GlkStyle, s), grid_put/clear, window_clear, flush); `glk::{WinType, GlkStyle}`.
- `crates/blorb`: `Blorb`, `ExecKind::{ZCode, Glulx}`; `crates/app/src/hints.rs::extract_story` (errors on Glulx today).
- `crates/app/src/session.rs`: `GameSession` (impl `Engine`), `TurnResult`. Session is constructed and boxed in `main.rs` (~943).

## Global Constraints

- The Z-machine path stays **byte-identical** (3b-i tests + render equivalence remain green); the renderer reproduces the 2-node case exactly.
- 0 warnings (`cargo build`, `cargo doc -p app --no-deps`) + full `cargo test --workspace` green per task.
- `app` may now depend on `gvm` + `blorb` (add to `crates/app/Cargo.toml`). Glk specifics live only in `GlulxSession`/the app `GlkBackend`; the trait surface stays engine-neutral.
- Commit-only on the phase's worktree branch; one commit per task (TDD). No push. Do not edit `TODO.md`.
- Commit trailers, every commit (no backticks in the body):
  `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
  `Claude-Session: https://claude.ai/code/session_01Uvf2RNUS7SBZHXPWqcRAkV`

---

## Task 1: `.gblorb`/`.ulx` routing + engine selection

**Files:** `crates/app/Cargo.toml`, `crates/app/src/hints.rs`, `crates/app/src/picker.rs`, the session-construction site in `main.rs`.

- [ ] **Step 1:** Add `gvm`/`blorb` deps to `crates/app/Cargo.toml`.
- [ ] **Step 2:** Change `extract_story` to return the executable **kind + bytes** (e.g. `enum LoadedStory { ZCode(Vec<u8>), Glulx(Vec<u8>) }`) — a `.gblorb`/raw `.ulx` yields `Glulx`; `.zblorb`/raw `.z*` yields `ZCode`. Add `.ulx`/`.gblorb` to the picker extensions.
- [ ] **Step 3: Failing test:** routing maps a GLUL Blorb → `Glulx`, a ZCOD Blorb and raw `.z5` → `ZCode`.
- [ ] **Step 4:** Implement; keep all existing load tests green (`.z5`, zip, `.zblorb`). Commit — `feat(app): route Glulx executables (.gblorb/.ulx) distinctly from Z-code`.

(Session creation wires to `GlulxSession` in Task 4; Task 1 just classifies.)

---

## Task 2: `BufferWindow` content + the app `GlkBackend`

**Files:** `crates/app/src/engine.rs` (extend `BufferWindow`), new `crates/app/src/glk_backend.rs`.

- [ ] **Step 1: Extend `BufferWindow`** to carry content for multi-window rendering: lines + per-line style runs + scroll, **plus a flag/marker** for "this is the primary window mirrored by the app transcript" (so the main window reuses `state.transcript` with search/persistence, and extra buffers carry inline content). Keep the Z-machine 2-node case unaffected (its single buffer stays the transcript).
- [ ] **Step 2: Failing tests** for an app `GlkBackend` (`AppGlk`): `window_open`/arrange builds the right `WinNode` tree (grid + buffer; a 3-window split); `put_text(win, GlkStyle, s)` appends to the buffer window with the style mapped to text-style bits (Emphasized→bold, Header→bold, etc.); `grid_put`/`grid_clear` update a `GridWindow`'s cells/cursor; the primary buffer's new text is drainable for `take_transcript`.
- [ ] **Step 3: Implement** `AppGlk` maintaining the `ScreenModel` tree + per-window buffers; `GlkStyle → u8` style-bit mapping (reuse the transcript run bits). Commit — `feat(app): app GlkBackend mapping Glk windows to the neutral ScreenModel`.

---

## Task 3: `GlulxSession` implements `Engine`

**Files:** new `crates/app/src/glulx_session.rs`, `engine.rs`/`lib` exports.

- [ ] **Step 1: Failing tests** (hand-assembled `.ulx` via gvm's reach, or a small fixture): a Glulx program that opens grid+buffer, prints, and `glk_request_line_event`+`glk_select` → `GlulxSession::new(bytes)` then `submit("hi")` returns a `TurnResult` whose transcript = the program's output and `transcript_runs` carry any Glk emphasis; `screen()` is the expected 2-window tree; `submit_key(KeyInput::Up)` delivers the Glk keycode for Up to a pending char request; `save_state()`→`restore_state()` round-trips under the `"glulx"` tag; `introspect()`/`current_location()` are `None`.
- [ ] **Step 2: Implement** `GlulxSession`: hold `gvm::Machine::with_glk(mem, Box<AppGlk>)` + pending-input state; `submit`/`submit_key` deliver input then **drive the gvm step loop** (`step` until `NeedLine`/`NeedChar`/`Quit`, like gvm-cli's `drive`), draining `AppGlk` output into the `TurnResult` (transcript + runs); `key_to_glk(KeyInput) -> Glk keycode`; `screen()` reads `AppGlk`'s tree; `save_state`/`restore_state` wrap `gvm` save bytes + the `"glulx"` tag; `introspect`/`current_location` → `None`; `beep`/`location_method` → `None`. Commit — `feat(app): GlulxSession (Engine over gvm + the app GlkBackend)`.

---

## Task 4: Generic multi-window renderer + wire Glulx into the app

**Files:** `crates/app/src/render/` (the screen renderer), `crates/app/src/main.rs`.

- [ ] **Step 1: Failing tests:** a renderer over a `ScreenModel` tree splits a rect for a `Pair` (vertical/horizontal, ratio/fixed) and renders `Grid` leaves (cells + viewport) and `Buffer` leaves (wrapped/styled, via the transcript renderer) into the right sub-rects; the **Z-machine 2-node tree renders byte-identical** to the current output (reuse/extend the 3b-i equivalence tests).
- [ ] **Step 2: Implement** the recursive tree renderer for the story pane (the map keeps its pane; the engine is told the story-pane size each frame). The single-grid+single-buffer case routes to the existing grid/transcript draw (no change for Z-machine); multiple/other windows use the generic path.
- [ ] **Step 3: Wire Glulx into `main.rs`:** at session creation, the Task-1 routing builds a `GlulxSession` (boxed as `Engine`) for Glulx, `GameSession` for Z-code. The turn loop already drives `Box<dyn Engine>`. Confirm: loading a Glulx story plays via `submit`/`submit_key`, renders its windows, the map pane is present but quiet (location `None`).
- [ ] **Step 4: End-to-end smoke** (a small real or hand-built `.ulx`/`.gblorb`): load it, submit a couple of commands, assert sane non-empty transcript output and no panic. Commit — `feat(app): generic multi-window renderer + Glulx games play in the TUI`.

---

## Self-review checklist (run before final review)

- A `.gblorb`/`.ulx` loads a `GlulxSession` and plays; a `.zblorb`/`.z5` still loads `GameSession` unchanged.
- The Z-machine path is byte-identical (3b-i tests + equivalence tests green); the new renderer reproduces the 2-node case exactly.
- Glk styles render as transcript emphasis; multi-window Glulx layouts render as nested splits with the map alongside; graphics/blank windows are placeholders.
- Glulx saves are tagged `"glulx"`; the foreign-engine restore guard prevents cross-loading; `introspect`/`current_location` are `None` so play-aids/map degrade gracefully.
- The trait surface stayed engine-neutral (Glk specifics only in `GlulxSession`/`AppGlk`).
- 0 warnings; full `cargo test --workspace` green.
